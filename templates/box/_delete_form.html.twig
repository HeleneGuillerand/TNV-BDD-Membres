<form method="post" action="{{ path('app_box_delete', {'id': box.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce caisson?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ box.id) }}">
    <button class="btn btn-danger">
        <i class="fa fa-trash-o"></i>
    </button>
</form>
