<form method="post" action="{{ path('app_peculiarity_delete', {'id': peculiarity.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette particularité?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ peculiarity.id) }}">
    <button class="btn btn-danger">
        <i class="fa fa-trash-o"></i>
    </button>
</form>
